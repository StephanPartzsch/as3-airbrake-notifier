project.title		= as3-airbrake-notifier
project.version		= 1.0.0
binary.name			= ${project.title}-${project.version}

repository.owner	= StephanPartzsch

flex.bin.dir 		= ${flex4sdk.dir}/bin
flex.lib.dir 		= ${flex4sdk.dir}/frameworks/libs

flex.locale 		= en_US
flex.locale.dir 	= ${flex4sdk.dir}/frameworks/locale/${flex.locale}

asdoc 				= ${flex.bin.dir}/asdoc${EXECUTABLE_EXTENSION}
compc	 			= ${flex.bin.dir}/compc${EXECUTABLE_EXTENSION}

target.player		= 10.0
playerglobal.swc	= ${flex.lib.dir}/player/${target.player}/playerglobal.swc

script.dir			= ${basedir}
src.dir				= ${basedir}/../src
deploy.dir			= ${basedir}/../bin
libs.dir			= ${basedir}/../lib